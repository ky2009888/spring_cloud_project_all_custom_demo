server:
  port: 80
spring:
  application:
    name: cloud-consumer-order80-consul
  #consul客户端的配置
  cloud:
   consul:
     host: localhost
     port: 8500
     discovery:
      service-name: ${spring.application.name}

